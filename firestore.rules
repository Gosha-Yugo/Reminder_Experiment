rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 認証ユーザーのみがアクセス可能
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}